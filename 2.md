```mermaid
graph TD
    subgraph 训练阶段
        A1[公开数据集] --> B[训练语料库构建];
        A2[小规模自采数据] --> B;
        
        subgraph 数据增强模块
            C1[轻量化模拟策略] --> C2[合成错误语料对];
        end
        
        C2 --> B;

        B --> D[PEFT LoRA 微调];
        E[基础语言模型] -- 冻结参数 --> D;
        D -- 训练适配器 --> F[训练完成的校正模型];
    end

    subgraph 推理与应用阶段
        G[含错误的ASR文本] --> H[推理引擎];
        E -- 加载 --> H;
        F -- 加载 --> H;
        H --> I[校正后文本];
    end
    
    subgraph 性能与效率评估
        I --> J[校正精度评估];
        H --> K[推理效率分析];
    end
